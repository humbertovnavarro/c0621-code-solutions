/* exported isLowerCased */
/*
  if word is equal to word but lowercase,
    return true
  otherwise
    return fase
*/
function isLowerCased(word){
  if(word === word.toLowerCase()){
    return true;
  }
  else{
    return  false;
  }
}
